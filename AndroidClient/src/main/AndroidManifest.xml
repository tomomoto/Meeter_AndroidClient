<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.location.gps" />
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme"
        android:usesCleartextTraffic="true"
        >
        <!-- TODO remove^ android:usesCleartextTraffic="true" when start usage of https -->

        <activity
            android:name=".context.start.activity.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".context.login.activity.LoginActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.tom.meeter.context.start.activity.StartActivity"/>
        </activity>
        <activity
            android:name=".context.profile.activity.ProfileActivity"
            android:label="@string/title_activity_activity_two"
            android:parentActivityName=".context.login.activity.LoginActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.tom.meeter.context.login.activity.LoginActivity" />
        </activity>
        <activity
            android:name=".context.registration.activity.RegistrationActivity"
            android:label="@string/RegistrationActivityTitle"
            android:parentActivityName=".context.login.activity.LoginActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.tom.meeter.context.login.activity.LoginActivity" />
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCPeLeAy-WAKZmeeMG2jhJ9w-oTLm8Wk40" />

        <service
            android:name=".context.network.service.SocketIOService"
            android:enabled="true"
            android:exported="false"/>
        <service
            android:name=".context.gps.service.LocationTrackerService"
            android:enabled="true"
            android:exported="false"/>

    </application>

</manifest>
